#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<component name="ArtifactManager">
  <artifact type="war" name="${artifactId}al_server:war">
    <output-path>${symbol_dollar}PROJECT_DIR${symbol_dollar}/target</output-path>
    <root id="archive" name="${artifactId}al-${version}.war">
      <element id="artifact" artifact-name="${artifactId}al_server:war exploded" />
    </root>
  </artifact>
</component>