#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<component name="ArtifactManager">
  <artifact type="war" name="${artifactId}-server:war">
    <output-path>${symbol_dollar}PROJECT_DIR${symbol_dollar}/target</output-path>
    <root id="archive" name="${artifactId}-${version}.war">
      <element id="artifact" artifact-name="${artifactId}-server:war exploded" />
    </root>
  </artifact>
</component>